.new_session
  %h1 Sign Up Restaurant

= form_tag restaurants_path do

  = label :restaurant, :name, 'Restaurant Name', :id => 'label'
  = text_field :restaurant, 'name', placeholder: "Restaurant Name", :id => 'textfield'
  
  = label :restaurant, :email, 'Access Email', :id => 'label'
  = text_field :restaurant, 'email', placeholder: "Access Email", :id => 'textfield'
  

  = submit_tag 'Request Account', :id => 'login_submit'